# اسکنر IP کلودفلر برای ایران 🇮🇷

 یک ابزار ساده برای پیدا کردن IP های سالم کلودفلر که در ایران کار می‌کنند. ( اختلالات دی ماه 1404 )

## ویژگی‌ها

- اسکن سریع با چند Thread همزمان ( 2 میلیون آیپی ~ 5 دقیقه ) 
- تست اتصال TLS/SSL
- اندازه‌گیری سرعت و پینگ
- ذخیره نتایج در فایل JSON و TXT
- قابل تنظیم از طریق فایل config

## نصب و اجرا

```bash
# کلون کردن پروژه
git clone https://github.com/YOUR_USERNAME/CFScanTolidIRAN.git
cd CFScanTolidIRAN

# اجرای برنامه
python cf_scanner.py
```

## تنظیمات

فایل `config.json` را ویرایش کنید:

```json
{
  "test_domain": "chatgpt.com",
  "timeout": 2,
  "max_workers": 1500,
  "port": 443
}
```

یا لیست IP ها را در فایل `subnets.txt` قرار دهید (هر سابنت در یک خط).

## خروجی

- `working_ips.json` - نتایج کامل با جزئیات
- `working_ips.txt` - لیست ساده IP ها

## نیازمندی‌ها

- Python 3.6+
- بدون نیاز به نصب کتابخانه اضافی

## نویسنده

@AghaFarokh

## لایسنس

MIT
